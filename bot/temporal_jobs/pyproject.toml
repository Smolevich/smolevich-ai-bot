[project]
name = "smolevich-bench-worker"
version = "0.1.0"
description = "Temporal worker running the free-model benchmark"
requires-python = ">=3.12"
dependencies = ["temporalio==1.30.0"]

[tool.uv]
package = false
